name: HW5a
conda_env: conda.yaml
entry_points:
    main:
        parameters:
            train_path: string
            model_param1 : {type: float, default: 0.3}
        command: |
        python train.py \
          --train_path={train_path} \
          --model_param1={model_param1}
          